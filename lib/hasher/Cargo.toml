[package]
name = "hasher"
version = "0.1.0"
edition = {workspace = true}
license = {workspace = true}
description = "custom sha256 hasher"

[dependencies]
sha2 = {workspace = true}

[lints.rust.unexpected_cfgs]
level = "allow"
check-cfg = [
    'cfg(target_os, values("solana"))'
]