[package]
name = "hashset"
version = "0.1.0"
edition = {workspace = true}
license = {workspace = true}
description = "hashset"

[lib]
crate-type = ["rlib"]

[dependencies]
hasher = {workspace = true}
